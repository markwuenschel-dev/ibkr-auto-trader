// Tailwind CSS v4 is configured through its PostCSS plugin (no tailwind.config.js needed — v4 is
// CSS-first; theme lives in app/globals.css via @theme).
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
