"""strategy — RebalancerStrategy (PT-11): target allocation + drift thresholds; emits StrategyIntent ONLY.

A strategy proposes; it never sizes, approves, or executes. The SignalSource/SignalStrategy seam (ML
path) is declared here but left unimplemented for the paper milestone. Empty until PT-11.
"""
